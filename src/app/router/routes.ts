import { Routes } from '@angular/router';
import { PlanComponent } from '../core/components/plan/plan.component';

export const routes: Routes = [
  {
    path: '',
    component: PlanComponent,
  },
];
